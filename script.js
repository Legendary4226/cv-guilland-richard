// Permet lors du clic sur le bouton d'exécuter cette fonction, elle change la classe de body à l'inverse de son état actuel
function toggle_dark_light_theme() {
    document.documentElement.classList.toggle("white-theme");
}